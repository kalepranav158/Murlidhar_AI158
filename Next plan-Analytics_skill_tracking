✅ PHASE 1 — PRODUCTION HARDENING

These are mandatory before freeze.

1️⃣ Gate Debug Endpoints
Current State

Debug routes always mounted.

Required Fix

In app/main.py:

import os

DEBUG_ENDPOINTS_ENABLED = os.getenv("DEBUG_ENDPOINTS", "false").lower() == "true"

if DEBUG_ENDPOINTS_ENABLED:
    app.include_router(debug.router)
Freeze Criteria

DEBUG_ENDPOINTS=false in production

Verified 404 on /debug/... in prod mode

2️⃣ Lock Composite Config Validation

Ensure this exists in analytics_config.py:

assert abs(sum(COMPOSITE_CONFIG.values()) - 1.0) < 1e-6
Freeze Criteria

Config weights validated at startup

App crashes if invalid weights

3️⃣ Remove or Archive Legacy Mastery Tables

You mentioned:

alankar_mastery

phrase_mastery

Choose One:

Hard delete migration
OR

Rename to _legacy_* and block writes

Freeze Criteria

Only skill_progress is authoritative

✅ PHASE 2 — SYSTEM CONSISTENCY TESTS

These verify entire flow, not modules.

4️⃣ Add One Full Integration Test

Test Flow:

submit practice
→ save_session
→ update_skill_progress
→ unlock triggered
→ curriculum updated
→ streak updated
→ analytics snapshot saved
Freeze Criteria

Single test verifies entire chain

Passes locally

5️⃣ Duplicate Submission Stress Test

Test:

Submit same session twice

Expect:

First: saved

Second: duplicate_rejected

successful_sessions not incremented

Freeze Criteria

Idempotency verified

✅ PHASE 3 — DATA INTEGRITY VALIDATION

These ensure long-term stability.

6️⃣ Unlock Integrity Audit Script

Run for all users:

verify_unlock_integrity(user_id, skill_id)
Freeze Criteria

No violation

No unlocked skill with <3 successful sessions

7️⃣ Analytics Window Check

Query:

SELECT COUNT(*) FROM analytics_snapshots WHERE user_id=...
Freeze Criteria

Max 30 per skill

Oldest pruned first

8️⃣ Streak Boundary Simulation

Simulate:

Same day double submit

Next day increment

2-day gap reset

Freeze Criteria

Correct streak behavior

✅ PHASE 4 — PERFORMANCE SANITY

Not optimization — sanity.

9️⃣ Load Simulation (Basic)

Simulate:

20 consecutive sessions

Verify:

No DB lock errors

No duplicate increments

No unlock regression

🔟 DB Constraint Verification

Confirm constraints exist:

unlock timestamp check

session_hash uniqueness

composite_score bounds (if defined)

✅ PHASE 5 — DOCUMENTATION FREEZE

Lock these files:

SYSTEM_CORE.md

ARCHITECTURE.md

IMPLEMENTATION_SUMMARY.md

CHANGELOG.md

Add:

## v1 Backend Freeze
Date:
All freeze checklist items completed.
🎯 BACKEND v1 FREEZE CRITERIA SUMMARY

System must satisfy:

Domain	Required State
Evaluation	Deterministic
Technique	Compared vs expected
Composite	Configurable, validated
Mastery	3-session threshold enforced
Unlock	Forward-only immutable
Streak	Timezone safe
Analytics	Rolling window capped
Duplicates	Idempotent
Debug	Gated by env
Tests	Integration test passes
🧠 AFTER FREEZE — WHAT YOU DO NOT TOUCH

Once frozen:

Do not change composite weights casually

Do not alter unlock threshold

Do not alter streak logic

Do not change rolling window size

Do not refactor skill_progress schema

Only additive changes allowed.